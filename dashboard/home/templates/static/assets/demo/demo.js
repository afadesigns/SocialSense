const type = ['', 'info', 'success', 'warning', 'danger'];

const demo = {
    initPickColor() {
        // code for initPickColor
    },

    checkFullPageBackgroundImage() {
        // code for checkFullPageBackgroundImage
    },

    initFullCalendar() {
        // code for initFullCalendar
    },

    initDocChart() {
        // code for initDocChart
    },

    initChartPageCharts() {
        // code for initChartPageCharts
    },

    initDashboardPageCharts() {
        // code for initDashboardPageCharts
    },

    showSwal(type) {
        // code for showSwal
    },

    initNowUiWizard() {
        // code for initNowUiWizard
    },

    initSliders() {
        // code for initSliders
    },

    initVectorMap() {
        // code for initVectorMap
    },

    initGoogleMaps() {
        // code for initGoogleMaps
    },

    initSmallGoogleMaps() {
        // code for initSmallGoogleMaps
    },

    showNotification(from, align) {
        // code for showNotification
    },
};

// Initialize components
demo.initPickColor();
demo.checkFullPageBackgroundImage();
demo.initFullCalendar();
demo.initDocChart();
demo.initChartPageCharts();
demo.initDashboardPageCharts();
demo.showSwal('info');
demo.initNowUiWizard();
demo.initSliders();
demo.initVectorMap();
demo.initGoogleMaps();
demo.initSmallGoogleMaps();
demo.showNotification('top', 'right');
